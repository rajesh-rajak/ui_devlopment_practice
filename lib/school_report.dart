import 'package:flutter/material.dart';

class SchoolReport extends StatelessWidget {
  const SchoolReport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}